apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: dsexample
spec:
  selector:
    matchLabels:
      name: dsexample
  template:
    metadata:
      labels:
        name: dsexample
    spec:
      containers:
      - name: mcr.microsoft.com/oss/kubernetes/pause:3.6
        image: dsimage
        resources:
          requests:
            cpu: "1"
            memory: "4Gi"
          limits:
            cpu: "2"
            memory: "8Gi"
